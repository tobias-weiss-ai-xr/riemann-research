{
  "_comment": "Worker roster for the opendesk-nix Matrix appservice campaign (MA-1..MA-3). Each worker is a (provider, model) pair verified in ~/.pi/agent/models.json. The orchestrator dispatches at most one task per worker at a time — 3 workers = 3 parallel jobs.",
  "defaults": {
    "accept_timeout_s": 2400,
    "max_attempts": 3,
    "retry_delay_s": 60,
    "agent_timeout_s": 7200,
    "no_op_max_attempts": 2,
    "prompt_template": "prompts-matrix/worker.md"
  },
  "workers": [
    {
      "name": "opencode-go",
      "provider": "opencode-go",
      "model": "deepseek-v4-pro",
      "endpoint": "https://opencode.ai/zen/go/v1",
      "note": "opencode.de zen/go — the proven ICS-campaign provider (IC-1 used it)",
      "enabled": true,
      "max_concurrent": 1
    },
    {
      "name": "zai",
      "provider": "zai",
      "model": "glm-5.2",
      "endpoint": "https://api.z.ai/api/coding/paas/v4",
      "note": "Z.AI GLM 5.2 — strong reasoning for config surgery",
      "enabled": true,
      "max_concurrent": 1
    },
    {
      "name": "saia",
      "provider": "saia",
      "model": "deepseek-v4-flash-0731",
      "endpoint": "https://chat-ai.academiccloud.de/v1",
      "note": "Academic Cloud DeepSeek V4 Flash — fast, text-heavy",
      "enabled": true,
      "max_concurrent": 1
    }
  ]
}
